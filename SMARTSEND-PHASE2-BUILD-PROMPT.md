# פרומפט בנייה — שלב ב' של השליחה החכמה

> **איך משתמשים:** צ'אט חדש → `קרא את SMARTSEND-PHASE2-BUILD-PROMPT.md ופעל לפיו`.
> נכתב 23/07/2026 בערב. **גרסה 2** — נכתב מחדש אחרי שארבעה מומחים ביקרו את גרסה 1 ומצאו שהיא לא בת-ביצוע. כל עובדה כאן אומתה מול הקוד, הסכמה החיה או `node_modules`.

---

## 0. הגדרת הסקופ — קרא את זה קודם

**המסמך הזה מכסה סשן אחד: פריטים 1-5 למטה. ריאקט טהור.**
**‏אין בו DB, אין בו API, אין בו פריסה, ואין בו הפיכת דגלים.**

| מחוץ לסקופ | למה |
|---|---|
| **טאב "שליחה חכמה" במסך המקורות** | **הוכרע שייבנה** — אבל דורש SP + endpoint + הכרעת סכמה. **סשן ב'.** האפיון המלא ב-§7 |
| **‏`USE_SEND_MOCK`** | **לא לגעת.** זה צעד שחרור, לא צעד בנייה. ראה §6 |
| **‏`08b`, ‏`07`, ‏`08`** | לא חלק מהמשימה |
| **פריסה כלשהי** | עידן מבצע |

---

## 1. 🔴 כללים קשיחים

1. **אסור להריץ שום דבר על שום DB.** לא `sqlcmd`, לא `SELECT`, לא בדיקת חיבור.
2. **אסור לפרוס.** ‏commit ו-push בלבד.
3. **אסור לשנות `USE_SEND_MOCK`.** ‏§6 מסביר למה.
4. **אסור ללחוץ "שלח לכולם"** בשום בדיקה ידנית.
5. **חוזה קפוא לפני פיזור בנאים** — `PARALLEL-BUILD-SKILL.md`. כל קובץ בבעלות בנאי אחד.

---

## 2. עותק העבודה — נקבע, אין מה לשאול

```
C:\Subversions\NewBitBucket\PulseemReact    ענף NewClalDesign   HEAD 4807e1d3c   נקי
C:\Subversions\NewBitBucket\WebSiteApi      HEAD 5309866e
```

`SEND-TESTING-KICKOFF.md:40` מפנה ל-`C:\BitBucketProjects\*` — **אלה לא קיימים במכונה הזו** (המכונה היא `IdanNewPC`, לא `DESKTOP-SGGOMUO` שבקובץ). ‏`NewBitBucket\WebSiteApi` נמצא ב-`5309866e` — **בדיוק הקומיט שה-kickoff מייחס לעותק האחר.**

**‏`ApiForReactCode\` הוא מירror מיושן** (‏22/07 11:41 מול 12:58 בעותק הבונה, ומתפצל בשורה 69: `FeatureTierLogic` מול `accountFeatures.Contains(76)`). **לקריאה על ה-API — רק `NewBitBucket\WebSiteApi`.**

**אמת בעצמך לפני שאתה נוגע:** `git log --oneline -3` + `git status`.

---

## 3. המשימה — 5 פריטים

### פריט 1 · 🐛 באג קיים: שינוי עמודות עסקיות אובד בשקט

**זה הפריט בעל הערך הגבוה ביותר במסמך, והוא באג ולא פיצ'ר.**

`SmartSendScreen.tsx` מסמן `dirty` בעריכת מיפוי שדות (`:335`) — **אבל לא בעריכת `BusinessColumnsPicker`** (`:343`, עמודות מפקח / פער / מיון).

**התוצאה היום:** משתמש בוחר עמודת מפקח, יוצא, והבחירה אובדת. **בלי כיתוב, בלי אזהרה, בלי כלום.**

**התיקון:** לסמן `dirty` גם ב-`onChange` של `BusinessColumnsPicker`.

---

### פריט 2 · 🐛 `openSummary` דורך את הקמפיין לפני כל אישור

`SmartSendScreen.tsx:229-236` מריץ, **לפני שהדיאלוג נפתח**:
1. `doSave(true)` — מחבר את הקבוצה הסינתטית ל-`GroupIds` **האמיתי** של הקמפיין
2. `fillAndSummarize` — כותב את כל הנמענים לתוך הקבוצה

**כלומר לחיצה על "שלח לכולם" — רק כדי לראות את הסיכום — כבר משנה למי הקמפיין שולח בפרודקשן.** נטישת הדיאלוג משאירה את הקמפיין דרוך, ושליחה מאוחרת מהאשף הקלאסי תוציא אותו.

**התיקון:** להעביר את שניהם ל**אחרי** אישור המשתמש בדיאלוג.

**‏⚠️ זה פגם עצמאי — הוא שורד את `08b` וצריך להישלח בכל מקרה.** אל תתייחס אליו כאמצעי זמני.

---

### פריט 3 · אזהרת יציאה עם שינויים שלא נשמרו

**‏⚠️ אילוץ מאומת ב-`node_modules`:** ‏`package.json:60` מצהיר `^6.3.0`, אבל **המותקן בפועל הוא `react-router-dom@6.4.2`** (`node_modules/react-router-dom/package.json:3`). ‏**6.4.2 לא מייצא שום blocker** — ‏`grep -rl useBlocker` בתוך `react-router-dom/dist` ו-`react-router/dist` מחזיר **0**. ה-blocker נחת ב-**6.7.0**. **אל תחפש אותו.**

**מה לבנות:**
1. **סרגל שמירה דביק בתחתית המסך** כשיש שינויים לא שמורים, במקום הכיתוב האפור בכותרת (`:402-409`) שרחוק מכפתור השמירה (`:353`).
2. **יירוט של יציאות המסך.** היציאה האמיתית היחידה היא כפתור החזרה לבורר (`:420`) — `:446` כבר אחרי שמירה.
3. **דיאלוג תלת-כיווני חובה:** *שמור וצא* / *צא בלי לשמור* / *ביטול*. **לא דו-כיווני** — השמירה נכשלת באמת (`:152`, `:178`, `:218`) ואסור שתנווט על כישלון.
4. **`beforeunload`** לסגירת טאב. תקדים: `src/screens/LegacyPage/LegacyPageFrame.tsx:23`, וגם `ClientSearchResult.js:211`.

**‏⚠️ סתירה לתקן:** `SmartSendScreen.tsx:402` מכיל היום *"Text only, by contract: no router blocker and no beforeunload handler"*. בעל המוצר הפך את ההכרעה במודע. **עדכן את ההערה.**

---

### פריט 4 · שליחת בדיקה — **לא** לשמור אוטומטית

> **הבקשה המקורית הייתה ששליחת בדיקה תשמור. הביקורת הפריכה אותה, ואני מקבל את ההפרכה.**

**למה לא:** שמירה מחברת את הקבוצה הסינתטית ל-`GroupIds` האמיתי (`:193`, `:210`). **כפתור שכתוב עליו "בדיקה" ישנה למי הקמפיין שולח בפרודקשן.** מי שיבדוק ויעזוב משאיר את הקמפיין מחובר למקור.

**והתקדים לא תקף:** ב-`openSummary` השמירה היא **תנאי מוקדם** — `fillAndSummarize` זקוק למיפוי שנשמר. לשליחת בדיקה אין צורך בזה, **והמייל יוצא ביט-זהה בין כה** (הקלון לא נושא מיפוי — `TestSendDialog.tsx:51` כבר מזהיר).

**מה לבנות במקום:** אם יש שינויים לא שמורים בלחיצה על "שליחת בדיקה" — דיאלוג: *"לשמור את המיפוי קודם? זה יחבר את נמעני המקור לקמפיין."* המשתמש מחליט. אותה תוצאה, בלי מוטציה נסתרת.

---

### פריט 5 · עיצוב — קמפיינים ככרטיסים

**המצב היום:** `CampaignPicker.tsx:81` מרנדר `flexDirection:'column'` — שורה מלאה לכל קמפיין, עם שלוש מילים בתוכה. זה מה שנראה רע.

**‏למה כרטיסים כן נכונים כאן, למרות 133 קמפיינים:** המתג `onlySendable` דלוק כברירת מחדל (`:127`) ומסנן ל-`Created && IsNewEditor` (`:64`) — **בפועל נשארות כמה טיוטות.** אף אחד לא רואה 133 במצב הרגיל.

**מה לבנות:**
- רשת עוטפת, כרטיס ברוחב **264** — להעתיק מ-`SourcePicker.tsx:29-31`. **לא** מ-`ChannelSelector.tsx:22-25` (שם `minWidth:150` והכרטיסים נמתחים).
- **תוכן הכרטיס:** שם (חיתוך ל-2 שורות) · צ'יפ סטטוס · `#מספר` · תאריך עדכון. **בדיוק מה שכבר מרונדר** ב-`:280-299`. **בלי מספר נמענים** — הוא תמיד 0 לקמפיין בר-שליחה.
- **‏🔴 להסיר `maxHeight: 420`** (`:84`). רשת שגוללת בתוך עצמה מסתירה את כפתור ההמשך.
- **‏🔴 קיצוץ ל-12 כרטיסים + "הצג עוד N".** בלי זה כיבוי המתג נותן 133 כרטיסים מושבתים — גרוע מהרשימה של היום.

**‏🔴 שתי טעויות בגרסה 1 של המסמך הזה, תקן אותן ואל תחזור עליהן:**
1. **הכפתור ב-`:375-380` הוא לא "הצג עוד".** הוא `setOnlySendable(false)` — **מתג סינון** שחושף שורות מושבתות. צריך **שני פקדים נפרדים** עם תוויות שונות: מתג הסינון, וכפתור ההרחבה של הקיצוץ.
2. **‏`Models/Enums/Campaign.ts:11-20` מגדיר 7 סטטוסים**, לא 3. ‏`CampaignPicker.tsx:41-47` כבר ממפה את כולם. אל תצמצם ל-1/2/4.

**‏🔴 מלכודת חובה:** מיפוי החצים ב-`:209-211` שונה **במכוון** לסמנטיקת רשימה אנכית (Down/Right = הבא). **ברגע שזו רשת — להחזיר לסמנטיקת `SourcePicker.tsx:180-181` ולהסיר את `aria-orientation="vertical"` מ-`:368`.** ההערות בקובץ מסבירות את ההיגיון הנוכחי — לעדכן אותן.

**לשמר:** `role="radiogroup"`, roving tabindex, Enter/רווח, כרטיסים מושבתים עם סיבה ב-`aria-describedby`, אי-גניבת פוקוס.

---

## 4. שיטת העבודה — מחייבת, לא המלצה

### 4.0 שני עקרונות שגוברים על הכל

**‏1. שימוש חוזר לפני בנייה. מינימום שינויים.**
כל קובץ חדש, קומפוננטה חדשה, ‏hook חדש, ‏SP חדש או endpoint חדש **חייב הצדקה כתובה** מול מה שכבר קיים. הקוד הזה עשיר בדפוסים — §4ב הוא מצאי חלקי. **אם בנית משהו חדש בלי לצטט מה בדקת וקיים ולמה הוא לא מתאים — עשית טעות.**

**‏2. חוויית המשתמש היא ממד מדורג, לא נספח.**
בעל המוצר ביקש במפורש דגש על נוחות. **מסך שעובד ולא נוח — נכשל.** בסבבי הביקורת חייב לשבת מבקר שתפקידו היחיד הוא UX, והוא מדווח בנפרד.

### 4.1 חקירה בלתי-תלויה — לפני שכותבים שורה

**‏אל תקרא קבצים לבד ואל תתחיל מהתכנית הזו כנתון.** הפעל `PARALLEL-RECON-SKILL.md` וצוותים מקבילים, כל אחד בזווית אחרת:

| צוות | מנדט |
|---|---|
| **מבנה** | ‏`SmartSendScreen`, ‏`CampaignPicker`, ‏`TestSendDialog`, ‏`SendSummaryDialog` — מי מחזיק מצב, איפה `dirty` נקבע ונצרך, מה זרימת השמירה בפועל |
| **מצאי לשימוש חוזר** | אילו קומפוננטות, ‏hooks, ‏סגנונות ומפתחות i18n **כבר קיימים** ועונים על 5 הפריטים. **הפלט שלו מחייב את הבנאים** |
| **תפרים** | מה נשבר בכל מסך אחר אם נוגעים בקבצים האלה — ‏`dataSources.list` משותף, ה-polling, ‏`newsletterSlice` המשותף |
| **‏UX** | לעבור על התכנית עצמה ולומר איפה היא לא נוחה — **לפני שבונים אותה** |

**החקירה רשאית לסתור את התכנית הזו.** אם צוות מוצא שפריט מנוסח לא נכון, שיש דרך פשוטה יותר, או שהוא כבר קיים — **זה ממצא, לא חוסר-משמעת.** להביא לעידן.

### 4.2 הקפאת חוזה

- מפת בעלות: **קובץ = בנאי אחד.** קובץ עם שני בעלים הוא באג בתכנית.
- **כל מפתחות ה-i18n מנוסחים מראש בשלוש השפות.** פריטים 3 ו-4 דורשים מחרוזות חדשות. **אל תיתן לבנאים להמציא** — בסשן קודם זה עבד רק כי במקרה אחד הבנאים סיים ראשון.
- כל בנאי מקבל את **החוזה כולו**, לא רק את החלק שלו.
- **סעיף STOP:** בנאי שנתקל בפער בחוזה **מדווח ולא מתקן**. בסשן קודם זה קרה 6 פעמים, וכל תיקון חד-צדדי היה נראה סביר ומייצר חוסר-עקביות שקטה.

### 4.3 בנייה ואימות תפרים

לא "לחבר" — **לאמת**: המפתחות שהקוד משתמש בהם הם אלה שנוצרו, הראוט שנרשם הוא זה שנבנה. ואז `npm run build-ssl` — חייב **exit 0**.

### 4.4 לולאת ביקורת — צוות טרי בכל סבב

**מינימום 3 מבקרים בסבב, כל אחד בזווית אחרת:**

| | |
|---|---|
| **אדוורסרי** | תפקידו **להפריך** את התיקונים, לא לאשר אותם |
| **רדיוס פגיעה** | מה השתנה למי שלא קשור לפיצ'ר |
| **‏UX** | האם זה **נוח**. לא האם זה עובד |

**עד 2 סבבים או עד 0 ממצאים — מה שקורה קודם.**

**‏⚠️ הנתון שמצדיק את זה:** בסשן הקודם הבנייה הייתה ירוקה, וסבב הביקורת מצא blocker · פעולה הרסנית מאחורי טקסט מרגיע · שליחה מוצלחת בלי שום אישור. **ירוק אומר שהקוד מתקמפל, לא שהוא נכון.**
**ובסבבים על `SourcePicker` — שניים מארבעת הממצאים נגרמו מהתיקונים עצמם.** מתקן מגן על התיקון שלו. לכן צוות טרי בכל סבב.

### 4.5 סיום

commit + push. **בלי פריסה.**

---

## 4א. שלושת סקילי המקביליות — איך משלבים אותם נכון

שלושה מסמכים בשורש הריפו, והם **היררכיה ולא רשימה**:

```
PARALLEL-DISPATCH-SKILL.md   ← ראוטר. רץ ראשון, בכל פעם מחדש
      ├── PARALLEL-RECON-SKILL.md   ← קריאה בלבד, 2+ אזורים
      └── PARALLEL-BUILD-SKILL.md   ← כתיבה מקבילה
```

**‏⚠️ `parallel-dispatch` אינו מותקן כסקיל** (רק `parallel-recon` ו-`parallel-build` נמצאים ב-`~/.claude/skills/`). **קרא אותו כקובץ** מ-`PARALLEL-DISPATCH-SKILL.md`.

### הכלל: dispatch רץ לפני **כל** פיזור, לא פעם אחת בהתחלה

לכל שלב יש החלטת ניתוב משלו:

| שלב | ההחלטה |
|---|---|
| חקירה (§4.1) | קריאה על 2+ אזורים → **recon** |
| בנייה (§4.2-4.3) | כתיבה מקבילה → **build** — חוזה קפוא + בעלות זרה |
| ביקורת (§4.4) | קריאה בלבד → **dispatch → recon**: סעיף `UNKNOWNS` חובה, `file:line` על כל טענה |

### 🔴 הכיול הקריטי למשימה הזו

`parallel-dispatch` §2 קובע: **סף כדאיות ~60-90 שניות לסוכן.** ומזהיר עם נתון מדוד — **משימה של 18 שניות שפוצלה ל-4 לקחה 80 שניות מול 18.3 שניות לבד.**

**‏5 הפריטים של §3 אינם שווי-גודל:**

| פריט | היקף | ניתוב |
|---|---|---|
| 1 · `dirty` בעמודות עסקיות | שורה אחת | **מתחת לסף** |
| 2 · `openSummary` | פונקציה אחת | **מתחת לסף** |
| 3 · אזהרת יציאה | סרגל + דיאלוג + יירוט | סוכן |
| 4 · שליחת בדיקה | דיאלוג + תנאי | קטן |
| 5 · כרטיסים | פריסה + קיצוץ + מקלדת | סוכן |

**‏`parallel-dispatch` §2 שורת "Many independent SUB-floor tasks": לאחד לדליים של ≥90 שניות ולפזר את הדליים.** ‏פריטים 1+2+4 הם דלי אחד (כולם ב-`SmartSendScreen.tsx` ו-`TestSendDialog.tsx`), פריטים 3 ו-5 עומדים בפני עצמם.

**אל תפזר סוכן לפריט.** ‏5 סוכנים ל-5 פריטים שניים מהם תיקוני שורה — זו בדיוק הטעות שהסקיל מזהיר מפניה, ובנוסף פריטים 1, 2 ו-4 נוגעים באותו קובץ — **שני בעלים לקובץ אחד = דריסה.**

### ההבחנה שקובעת כמה סוכנים

`parallel-dispatch` §3: **המטרה שונה לפי סוג העבודה.**

- **חקירה וביקורת** — המטרה היא **שימור הקונטקסט שלך**, לא זמן. כל טוקן שסוכן קורא הוא כותרת שנשמרה לך. ‏**N האופטימלי כאן גדול מ-N של הזמן** — פזר רחב.
- **בנייה** — המטרה זמן ונכונות. ‏**N* הוא תקרה, לא יעד.**

---

## 4ב. מצאי לשימוש חוזר — בדוק את אלה לפני שאתה בונה משהו

| צורך | מה כבר קיים |
|---|---|
| דיאלוג | `TestSendDialog.tsx` — שלד head/body/foot ב-MUI v4, בלי תלות ב-lab |
| באנר / הודעה | `components/InlineBanner.tsx` — `info` / `warning` / `error`, עם `role` ו-action |
| רשת כרטיסים | `SourcePicker.tsx:29-31` — `flexWrap` + `width: 264` |
| ‏radiogroup + roving tabindex | קיים **בשלושה** קבצים: `SourcePicker`, `CampaignPicker`, `ChannelSelector`. **אל תכתוב רביעי** |
| מיפוי סטטוס קמפיין | `CampaignPicker.tsx:41-47` — כל 7 הסטטוסים |
| מגירה / היסטוריה | `openVersions` → `VersionsHistoryDialog` (`DataSources.tsx:183-189`) |
| טבלה + פגינציה + חיפוש | `DataSources.tsx` — הדפוס המלא |
| קריאה ל-thunk מ-JS | `jsThunk` ב-`smartSendSlice.ts:144` |
| ‏join של מיפויים ל-SP חדש | `DataSources_Delete.StoredProcedure.sql:69-82` |
| מפתחות i18n | **לבדוק ב-`DataSources.json` לפני שמוסיפים.** ‏`send.cancel`, `send.close`, `DataSources.retry` ועוד קיימים |

**כלל:** אם אתה עומד לכתוב קומפוננטה, ‏hook או מפתח i18n — **חפש קודם**. בסבב ביקורת קודם נמצא מפתח שנוסף ומעולם לא חובר, ומפתח שני שהיה מת מהיום הראשון.

---

## 5. תנאי קבלה

| # | תנאי | איך בודקים |
|---|---|---|
| 1 | `dirty` נקבע גם בעריכת עמודות עסקיות | לשנות עמודת מפקח → הסרגל מופיע |
| 2 | `doSave` + `fillAndSummarize` רצים **אחרי** אישור הדיאלוג | לפתוח סיכום ולבטל → `GroupIds` לא השתנה |
| 3 | סרגל שמירה דביק במקום הכיתוב בכותרת | ויזואלי |
| 4 | דיאלוג יציאה **תלת-כיווני**, ולא מנווט על כישלון שמירה | לדמות כישלון → נשארים במסך |
| 5 | `beforeunload` פעיל כשיש שינויים | סגירת טאב מזהירה |
| 6 | שליחת בדיקה **לא** שומרת לבד; מציעה כשיש שינויים | ויזואלי |
| 7 | קמפיינים ברשת עוטפת, כרטיס 264 | ויזואלי |
| 8 | `maxHeight:420` הוסר · קיצוץ ל-12 + "הצג עוד" **בנפרד** ממתג הסינון | ויזואלי, בשני מצבי המתג |
| 9 | חצים בסמנטיקת רשת · `aria-orientation` הוסר | מקלדת |
| 10 | i18n — מפתחות זהים ב-en/he/pl, JSON תקין, ספירת עלים זהה | סקריפט |
| 11 | `npm run build-ssl` = exit 0, אפס שגיאות TS בקבצים שנגעת בהם | ריצה |
| 12 | הכל committed ו-pushed, **כלום לא פרוס**, `USE_SEND_MOCK` עדיין `true` | `git status` + grep |

---

## 6. 🔴 למה `USE_SEND_MOCK` לא נוגעים בו כאן

בגרסה 1 של המסמך הזה נכתב שיש להפוך אותו ל-`false`. **זו הייתה טעות** — היא סותרת שני מסמכים מחייבים:

- **`GO-LIVE-PLAN-2026-07-23.md` §6:** *"נשאר `true` עד ששלב ג' הושלם"* — ושלב ג' הוא `08b`, שמחוץ לסקופ.
- **`SEND-TESTING-KICKOFF.md` §3.2:** *"עובר ל-`false` **רק אחרי** שה-API של השליחה אומת כפרוס"* — עדיין "לא ידוע #4".

**וגם החוזה בקוד עצמו** (`smartSendSlice.ts:22-27`) אומר שקומיט ה-flip **מוחק** את `_mocks/smartSendMock.ts` ו-`grep` חייב להחזיר 0 — כלומר הוא קומיט נפרד ומוגדר, לא תוצר לוואי של משימת בנייה.

**ההפיכה היא צעד שחרור בפיקוח עידן, לא צעד בנייה.**

### מה שהמוק מגן עליו — ולמה זה חמור

עד ההפיכה, "שלח לכולם" קורא לפיקסצ'ר. אחריה **הוא שולח באמת**, ו-`08b` עדיין לא רץ → `##שדה##` גולמי לנמענים אמיתיים.

**‏🔴 ואין דרך חזרה:** אין בכל `WebSiteApi` שום endpoint של Stop / Cancel / Pause. ‏`eCampaignStatus.Stopped/Canceled` מוזכרים ב**אפס** מקומות בקוד. התרופה היחידה היא `UPDATE` ידני ב-SSMS, ורק לפני שהסנדר תופס את האצווה.

**‏🔴 והתצוגה המקדימה היא אנטי-הגנה:** `SmartSendPreview.tsx:20-39` מחליף את השדות **בצד הלקוח**. הדיאלוג מציג תצוגה מותאמת ומושלמת של מייל שיישלח גולמי — **הוא מרגיע את מי שעומד ללחוץ.**

### ההגנה שכן שווה לבנות — בסשן נפרד, בצד ה-API

חסימה **הקשורה לתנאי עצמו** ולא לדגל, ולכן מסירה את עצמה:

```
ב-DataSourcesSenderController.Send, לפני SendCampaign:
  האם OBJECT_DEFINITION(OBJECT_ID('dbo.GetEmailsWithExtAT')) מכיל 'CampaignsToDataSources'?
  אם לא → 405 SMARTSEND_SP_NOT_DEPLOYED
```

מתבטלת ברגע ש-`08b` רץ · חוזרת מעצמה ב-rollback · אין קומיט המשך שאפשר לשכוח · ~15 שורות, בדיקה קריאה-בלבד. **להביא לעידן להכרעה — לא לבנות ביוזמה.**

---

## 7. סשן ב' — טאב "שליחה חכמה" במסך המקורות

### 7.1 ההכרעה ולמה היא גוברת על הביקורת

**בעל המוצר הכריע: טאב נוסף במסך מקורות הנתונים.** ההכרעה גוברת על התנגדות הביקורת, בגלל עובדה שלמבקרים לא הייתה:

> **בהמשך יגיעו SMS ווואטסאפ. צריך מקום אחד שמרכז טבלה של שליחות חכמות בכל הערוצים.**

**למה זה מכריע:**
- **מסך הקמפיינים הוא אימייל-בלבד מעצם בנייתו** — `getNewslatterParentChildData` → `email/GetEmailCampaignsManagement`. קמפייני SMS יושבים ב-endpoint אחר (`smsCampaign/getAllSmsCampaigns`, `smsSlice.js:4`). **לרישום חוצה-ערוצים אין בית במסכי הקמפיינים.**
- **אזור מקורות הנתונים כבר אגנוסטי לערוץ** — למקור יש `HasEmailIdentity` וגם `HasCellIdentity`.
- **שליחה חכמה היא ישות שונה משליחה רגילה**, ומגיע לה משטח משלה.

ההתנגדות שכן נשארת תקפה — "ההצדקה של מחיקת מקור כבר קיימת" (`DataSources.tsx:202-204` מחזיר `409 LOCKED_BY_CAMPAIGNS`) — **נכונה אבל צרה**: היא עונה רק על רגע המחיקה, לא על "מה מיפינו" ולא על גרסאות מיושנות.

### 7.2 🔴 חסם סכמה שההכרעה חושפת — להביא לעידן לפני בנייה

```sql
PK_CampaignsToDataSources PRIMARY KEY CLUSTERED ([CampaignID] ASC)
-- ואין בטבלה שום עמודת Channel
```

הערוץ מועבר ל-`DataSources_FillCampaignGroup` כ-`@prm_Channel` בזמן ריצה — **ולא נשמר בשום מקום.**

**המשמעות לרודמאפ הרב-ערוצי:** קמפייני SMS הם ישות נפרדת עם מרחב מזהים משלה. ‏`CampaignsToDataSources.CampaignID` יהיה **דו-משמעי בין ערוצים** — קמפיין אימייל 123 וקמפיין SMS 123 מתנגשים על אותו PK.

**אי אפשר לבנות רישום אגנוסטי-לערוץ על טבלה בלי מבחין ערוץ.**

**שלוש אפשרויות — להכרעת עידן:**

| | מה | עלות |
|---|---|---|
| **א** | להוסיף `Channel tinyint NOT NULL DEFAULT 1` ולהרחיב את ה-PK ל-`(CampaignID, Channel)` | ‏ALTER על טבלה חיה + עדכון `_Get`/`_Set`/`FillCampaignGroup`. **עכשיו זה זול — עם דאטה בפרודקשן זה לא יהיה** |
| **ב** | טבלה נפרדת לכל ערוץ (`SmsCampaignsToDataSources`) | כפילות סכמה ו-SP לכל ערוץ |
| **ג** | לבנות את הטאב לאימייל בלבד ולדחות | הטאב יצטרך שכתוב כשה-SMS יגיע |

**המלצתי: (א), ועכשיו.** הטבלה ריקה כמעט, השינוי זול היום ויקר אחר כך, וזה בדיוק מה שהטאב נועד לחשוף.

### 7.3 אילוצי בנייה שהביקורת מצאה — תקפים בלי קשר להכרעה

**למסך אין טאבים היום** (אפס `Tabs` בכל `src/screens/DataSources/`). הוספת סרגל טאבים גוררת שלושה דברים שחייבים פתרון:

1. **`SetPageState` שומר עמוד וחיפוש אחד** תחת `PAGE_NAME` יחיד (`DataSources.tsx:109-115`) → צריך מצב נפרד לכל טאב, אחרת מעבר בין טאבים ידרוס את החיפוש.
2. **ה-polling כל 4 שניות** (`:133-164`) שייך לטבלת המקורות בלבד → **חייב לא לרוץ** כשהטאב השני פעיל.
3. **שני הטאבים יחלקו `subPage="dataSources"`** → הדגשת התפריט זהה. להחליט אם זה מקובל.

### 7.4 עובדות מאומתות לבנייה

- **כל העמודות קיימות.** אין צורך בטבלה חדשה — רק SP חדש.
- **‏`DataSources_Delete.StoredProcedure.sql:69-82` כבר עושה את ה-join הזה** ומחזיר `CampaignID, CampaignName, c.Status, ctd.DataSourceVersionID, v.VersionNumber`. **דפוס להעתיק, לא שדה בתולי.** הוא מסונן ל-`Status = 2` ומוחזר כמטען של בלוק ‎-2 — ה-SP החדש יסיר את הסינון ויוסיף עמודות.
- **‏7 סטטוסים**, לא 3 (`Models/Enums/Campaign.ts:11-20`); `CampaignPicker.tsx:41-47` כבר ממפה את כולם — להשתמש באותו מיפוי.
- **גרסה מיושנת:** צ'יפ **ענבר** (לא אדום — השליחה עדיין עובדת) בנוסח `V3 · V5 זמינה`, **הצ'יפ עצמו הוא הקישור** למסך המיפוי. **בלי נעילה-מחדש מהטבלה** — נעילה מחדש היא שמירה מחדש, והעמודות עשויות להשתנות בין גרסאות (`MappingMismatchBanner` קיים בדיוק לזה ועובד רק במסך המיפוי). להוסיף מסנן "מיושנות בלבד".
- **אין ניתוק.** `doSave` רק **דוחף** ל-`GroupIds` (`:193`), אף פעם לא מסיר. **הקבוצה הסינתטית יציבה לקמפיין** (`CampaignsToDataSources_Set:75` — `PulseemDS_{CampaignID}`, get-or-create), אז לא נצבר id ישן — אבל פעולת ניתוק חסרה לגמרי, וזה המקום הטבעי לה.

---

## 8. מלכודות מאומתות

| | |
|---|---|
| **‏201, לא 200** | `GetSendSettings` מחזיר **201** ב-`EmailController.cs:519` (‏`:517` הוא `if (settings != null)`) · `SetSendSettings` ב-`:599,:605` |
| **‏thunks מ-JS** | קריאה **עם ארגומנט** מ-TS ל-thunk ב-`newsletterSlice.js`/`campaignEditorSlice.js` נכשלת ב-`TS2554`. יש `jsThunk` ב-`smartSendSlice.ts:144`. בלי ארגומנט — תקין |
| **`sitePrefix`** | `string \| undefined` — תמיד בתוך template literal או `?? ''` |
| **`Redirect`** | `openNewTab` **חובה** (`Redirect.ts:7`) |
| **`DefaultScreen`** | דורש `containerClass` |
| **‏csproj ישן** | כל `.cs` חדש חייב `<Compile Include>` ידני (`PulseemSiteApi.csproj:262`) |
| **שגיאות בסיס** | 448 × `TS2786` + 1 × `TS17001` ב-`Editors/modals/ResponseModal.js`. **קיימות מראש, לא לתקן** |
| **שגיאות כתיב מכוונות** | `Authrization`, `HideRecipietns`, `showAnimtionCheck`, `getNewslatterParentChildData` |

---

## 9. שאלות לעידן

1. **האם 5 הפריטים של §3 הם "רשימת התיקונים" שהוזכרה ב-23/07** (`SEND-TESTING-KICKOFF.md:136`), או שיש עוד? — חוסם את סשן א'.
2. **הכרעת §7.2 — מבחין הערוץ.** ‏(א) להוסיף `Channel` ל-PK עכשיו · (ב) טבלה נפרדת לכל ערוץ · (ג) אימייל בלבד ולדחות. — **חוסם את סשן ב'.**
3. **§7.3.3** — מקובל ששני הטאבים ידליקו את אותו פריט תפריט?

---

## 10. 🔎 המסמך הזה עבר ביקורת — ומה נמצא בו

גרסה 1 נבדקה ע"י 4 מומחים ונמצאה **לא בת-ביצוע**. גרסה 2 נכתבה מחדש. מה שתוקן:

| מה היה | מה נמצא | מה עכשיו |
|---|---|---|
| "להפוך את `USE_SEND_MOCK` ל-`false`" | סותר את `GO-LIVE §6` ואת `KICKOFF §3.2` — שני מסמכים שהמסמך עצמו מורה לקרוא קודם | **הוצא מהסקופ.** §6 מסביר |
| "שליחת בדיקה תשמור" | כפתור "בדיקה" היה משנה את `GroupIds` בפרודקשן | **הוחלף** בהצעה למשתמש (§3 פריט 4) |
| "טאב במסך המקורות" | הביקורת התנגדה | **הוחזר** — הרודמאפ הרב-ערוצי גובר (§7.1) |
| "כבר קיים כפתור הרחבה" | שגוי — `:375-380` הוא מתג סינון | תוקן, ונדרשים **שני** פקדים (§3 פריט 5) |
| "`react-router` 6.3.0, blocker חזר ב-6.4" | המותקן **6.4.2**, ה-blocker נחת ב-**6.7.0** | תוקן (§3 פריט 3) |
| "אין SP שמחזיר רשימה" | `DataSources_Delete:69-82` כבר עושה את ה-join | תוקן (§7.4) |
| "3 סטטוסים" | 7 (`Campaign.ts:11-20`) | תוקן |
| "`EmailController.cs:517`" | ‏:517 הוא `if (settings != null)`; ה-201 ב-**:519** | תוקן (§8) |
| תלות מעגלית בתנאי הקבלה | נמדדו על תוצר שנוצר אחרי סגירת הסשן | **פוצל לשני סשנים** |

**שני באגים קיימים שהביקורת חשפה** נכנסו כפריטים 1 ו-2 של §3 — הם לא היו בבקשה המקורית.

**ותיקון לתיעוד קודם:** נכתב ב-`GO-LIVE` שהתצוגה המקדימה מוכיחה שהמיפוי עובד "מקצה לקצה". **לא נכון.** `SmartSendPreview.tsx:20-39` מחליף בצד הלקוח — היא מוכיחה שהמיפוי נפתר לערכים, **לא** שהסנדר יידע להחליף. זה בדיוק מה ש-`08b` נותן.
